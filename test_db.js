const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('backend/database.sqlite'); db.all('SELECT DISTINCT cliente FROM os_logistica WHERE cliente LIKE \'%STREET%\'', (err, rows) => { console.log(err, rows); db.close(); });
