const sqlite3 = require('sqlite3'); const db = new sqlite3.Database('backend/database.sqlite'); db.all('SELECT nome, departamento, cargo FROM colaboradores WHERE nome LIKE '%jul%'', (err, rows) => console.log(rows));
